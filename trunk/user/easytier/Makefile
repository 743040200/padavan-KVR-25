SRC_NAME = EasyTier
THISDIR = $(shell pwd)

all:

extract_test:

config_test:

clean:

romfs:
	$(ROMFSINST) $(THISDIR)/easytier-core /usr/bin/easytier-core
	$(ROMFSINST) $(THISDIR)/easytier-cli /usr/bin/easytier-cli
	$(ROMFSINST) $(THISDIR)/easytier.sh /usr/bin/easytier.sh